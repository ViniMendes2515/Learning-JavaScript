//String, number, undefined, null, boolean, symbol

const nome = 'Vini'; //String
const nome2 = "Vini"; //String
const nome3 = `Vini`; //String
const num1 = 10; // Number
const num2 = 20.35; // Number
const sobrenomeAluno = null; //Nulo -> não aponta pra local nenhum na memória
let nomeAluno; // undefined -> não aponta pra local nenhum na memória
let aprovado = false; //Boolean = true, false (lógico)

